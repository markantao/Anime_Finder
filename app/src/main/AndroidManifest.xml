<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mark.anime_finder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".march"></activity>
        <activity android:name=".gintama" />
        <activity android:name=".samurai" />
        <activity android:name=".attack" />
        <activity android:name=".hunter" />
        <activity android:name=".naruto" />
        <activity android:name=".drama" />
        <activity android:name=".comedy" />
        <activity android:name=".adventure" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>